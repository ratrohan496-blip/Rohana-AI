import React from 'react';

const LogoIcon: React.FC = () => (
    <div className="w-14 h-14 rounded-full bg-transparent flex items-center justify-center flex-shrink-0 shadow-lg border-8 border-[var(--c-solid)]">
    </div>
);

export default LogoIcon;